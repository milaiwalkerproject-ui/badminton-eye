---
phase: 05-hawk-eye-ai-and-premium
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BadmintonEye/BadmintonEye/Models/CalibrationProfile.swift
  - BadmintonEye/BadmintonEye/Services/VideoCaptureManager.swift
  - BadmintonEye/BadmintonEye/Views/CourtCalibrationView.swift
  - BadmintonEye/BadmintonEye/Views/ChallengeVideoView.swift
  - BadmintonEye/BadmintonEye/Views/LiveMatchView.swift
  - BadmintonEye/BadmintonEye.xcodeproj/project.pbxproj
autonomous: true
requirements: [HAWK-01, HAWK-02, HAWK-03]

must_haves:
  truths:
    - "User can tap a Challenge button in the live match toolbar after each rally"
    - "Challenge button appears only during inProgress match and auto-hides after 10 seconds"
    - "User can capture a 5-10 second video clip via in-app camera for challenge"
    - "User can select a pre-recorded video from photo library instead of live capture"
    - "User can tap 4 court corners to calibrate court boundaries, saved per venue"
    - "Calibration profiles persist across app launches via SwiftData"
  artifacts:
    - path: "BadmintonEye/BadmintonEye/Models/CalibrationProfile.swift"
      provides: "SwiftData model for court calibration with 4 corner points and venue name"
      contains: "@Model"
    - path: "BadmintonEye/BadmintonEye/Services/VideoCaptureManager.swift"
      provides: "AVFoundation video capture and photo library picker coordination"
      contains: "AVCaptureSession"
    - path: "BadmintonEye/BadmintonEye/Views/CourtCalibrationView.swift"
      provides: "4-corner tap calibration overlay on camera feed"
      contains: "CourtCalibrationView"
    - path: "BadmintonEye/BadmintonEye/Views/ChallengeVideoView.swift"
      provides: "Video capture/selection UI presented as sheet from Challenge button"
      contains: "ChallengeVideoView"
  key_links:
    - from: "BadmintonEye/BadmintonEye/Views/LiveMatchView.swift"
      to: "ChallengeVideoView"
      via: "Challenge button in toolbar triggers sheet presentation"
      pattern: "sheet.*ChallengeVideoView"
    - from: "BadmintonEye/BadmintonEye/Views/CourtCalibrationView.swift"
      to: "CalibrationProfile"
      via: "Saves 4 corner taps to SwiftData CalibrationProfile model"
      pattern: "CalibrationProfile"
---

<objective>
Build the court calibration system and video capture pipeline for Hawk Eye challenges, including the Challenge button integration into the live match toolbar.

Purpose: Establishes the input pipeline -- court geometry and video footage -- that the AI model and trajectory engine will consume in Plan 03. Without calibration and capture, there is nothing to analyze.

Output: CalibrationProfile SwiftData model, VideoCaptureManager service, CourtCalibrationView, ChallengeVideoView, and Challenge button wired into LiveMatchView.
</objective>

<execution_context>
@/Users/milai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/milai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hawk-eye-ai-and-premium/05-CONTEXT.md

<interfaces>
<!-- Existing code the executor needs -->

From BadmintonEye/BadmintonEye/ViewModels/LiveMatchViewModel.swift:
```swift
@Observable
final class LiveMatchViewModel {
    private(set) var state: MatchState
    var isMatchOver: Bool { state.matchPhase == .complete || state.matchPhase == .abandoned }
    func scorePoint(for side: Side)
    func undo()
    func abandonMatch()
}
```

From ScoringEngine/Sources/ScoringEngine/Types.swift:
```swift
public enum MatchPhase: String, Codable, Sendable, Equatable {
    case inProgress
    case complete
    case abandoned
}
```

From BadmintonEye/BadmintonEye/Models/SwiftDataModels.swift:
```swift
@Model
final class PersistedMatch {
    var id: UUID = UUID()
    // ... all optional/defaulted for CloudKit
}
```

From BadmintonEye/BadmintonEye/Views/LiveMatchView.swift:
```swift
// Top bar overlay has: Undo button | Spacer | Game info | Spacer | End match button
// Challenge button should be added between Game info and End match button
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalibrationProfile model and VideoCaptureManager service</name>
  <files>
    BadmintonEye/BadmintonEye/Models/CalibrationProfile.swift,
    BadmintonEye/BadmintonEye/Services/VideoCaptureManager.swift,
    BadmintonEye/BadmintonEye.xcodeproj/project.pbxproj
  </files>
  <read_first>
    BadmintonEye/BadmintonEye/Models/SwiftDataModels.swift,
    BadmintonEye/BadmintonEye/Services/AuthManager.swift,
    BadmintonEye/BadmintonEye/App/BadmintonEyeApp.swift,
    BadmintonEye/BadmintonEye.xcodeproj/project.pbxproj
  </read_first>
  <action>
Create CalibrationProfile.swift as a SwiftData @Model with these fields (all optional or defaulted for CloudKit compatibility, matching PersistedMatch pattern):
- id: UUID = UUID()
- venueName: String = ""
- cornerTopLeft: Data? (encoded CGPoint as JSON Data)
- cornerTopRight: Data?
- cornerBottomLeft: Data?
- cornerBottomRight: Data?
- createdAt: Date = Date()
- imageWidth: Double = 0 (capture resolution for coordinate normalization)
- imageHeight: Double = 0

Add a computed property `corners: [CGPoint]?` that decodes all 4 corner Data fields into CGPoints, returning nil if any corner is missing. Add a method `setCorners(_ corners: [CGPoint], imageSize: CGSize)` that encodes each CGPoint as JSON Data and stores image dimensions.

Add CalibrationProfile to the modelContainer schema array in BadmintonEyeApp.swift alongside PersistedMatch and Player.

Create VideoCaptureManager.swift as an @Observable class (NOT singleton -- instantiated per challenge) with:
- Properties: `capturedVideoURL: URL?`, `isRecording: Bool = false`, `recordingDuration: TimeInterval = 0`
- Method `startRecording()`: configures AVCaptureSession with .video input from back camera, .hd1280x720 preset, AVCaptureMovieFileOutput writing to temp directory. Starts a Timer publishing every 0.1s to update recordingDuration.
- Method `stopRecording()`: stops AVCaptureMovieFileOutput, sets capturedVideoURL to output file URL.
- Method `cleanup()`: removes temp video file from disk.
- Conform to AVCaptureFileOutputRecordingDelegate for didFinishRecordingTo callback.
- Auto-stop recording after 10 seconds maximum.

Add both new Swift files to project.pbxproj: PBXBuildFile, PBXFileReference, group membership in Models and Services groups, and Sources build phase.
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye" && xcodebuild build -project BadmintonEye/BadmintonEye.xcodeproj -scheme BadmintonEye -destination "platform=iOS Simulator,name=iPhone 16" -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CalibrationProfile.swift exists with @Model annotation and all 4 corner Data? fields
    - CalibrationProfile.swift contains `corners` computed property and `setCorners` method
    - VideoCaptureManager.swift exists with AVCaptureSession configuration and startRecording/stopRecording
    - VideoCaptureManager.swift contains 10-second auto-stop logic
    - BadmintonEyeApp.swift schema includes CalibrationProfile
    - Project builds successfully
  </acceptance_criteria>
  <done>CalibrationProfile persists court corners via SwiftData. VideoCaptureManager records up to 10s video clips to temp directory. Project builds.</done>
</task>

<task type="auto">
  <name>Task 2: CourtCalibrationView, ChallengeVideoView, and Challenge button in LiveMatchView</name>
  <files>
    BadmintonEye/BadmintonEye/Views/CourtCalibrationView.swift,
    BadmintonEye/BadmintonEye/Views/ChallengeVideoView.swift,
    BadmintonEye/BadmintonEye/Views/LiveMatchView.swift,
    BadmintonEye/BadmintonEye.xcodeproj/project.pbxproj
  </files>
  <read_first>
    BadmintonEye/BadmintonEye/Views/LiveMatchView.swift,
    BadmintonEye/BadmintonEye/Models/CalibrationProfile.swift,
    BadmintonEye/BadmintonEye/Services/VideoCaptureManager.swift,
    BadmintonEye/BadmintonEye/Views/ScorecardRenderer.swift,
    BadmintonEye/BadmintonEye.xcodeproj/project.pbxproj
  </read_first>
  <action>
Create CourtCalibrationView.swift:
- Full-screen camera preview using AVCaptureVideoPreviewLayer wrapped in UIViewRepresentable
- Instruction text overlay: "Tap the 4 court corners in order: top-left, top-right, bottom-right, bottom-left"
- On each tap, add a green circle (20pt diameter) at the tap location with a numbered label (1-4)
- After 4 taps: show "Recalibrate" and "Confirm" buttons
- "Confirm" saves corners to a CalibrationProfile via SwiftData modelContext, includes a TextField for venue name
- "Recalibrate" clears all dots and restarts
- @Environment(\.modelContext) for SwiftData access
- @Environment(\.dismiss) to close after save

Create ChallengeVideoView.swift as a sheet-presented view:
- @State var videoCaptureManager = VideoCaptureManager()
- @Query var calibrations: [CalibrationProfile] to check if calibration exists
- If no CalibrationProfile exists: show "Calibrate Court First" button that navigates to CourtCalibrationView
- If calibrated: show two options:
  1. "Record Clip" -- camera preview with record button (red circle), duration counter, auto-stops at 10s
  2. "Select from Library" -- PhotosPicker(matching: .videos) from PhotosUI
- After video obtained (recorded or selected): show "Analyze" button that will trigger Hawk Eye pipeline (wired in Plan 03, for now just store the URL in a @State property and show "Analysis coming soon" placeholder)
- Cancel button in toolbar to dismiss

Modify LiveMatchView.swift to add Challenge button:
- Add @State private var showChallengeSheet = false
- Add @State private var challengeCountdown: Int = 0
- Add @State private var challengeTimer: Timer? = nil
- In the top bar overlay, between the Game info VStack and the End match button, add a Challenge button:
  - Image(systemName: "eye.trianglebadge.exclamationmark") with "Challenge" text below
  - Styled like other toolbar buttons: .foregroundStyle(.white), .padding(10), .background(.black.opacity(0.4)), .clipShape(RoundedRectangle(cornerRadius: 8))
  - Show countdown badge when timer active: small yellow circle with remaining seconds
  - Button triggers .sheet(isPresented: $showChallengeSheet) presenting ChallengeVideoView
  - Only visible when viewModel.state.matchPhase == .inProgress
- After each point scored (detect via onChange of viewModel.state.currentGame.scoreA + scoreB), reset challengeCountdown to 10 and start a Timer that decrements every second. When countdown hits 0, hide the button.
- Challenge button disabled (opacity 0.4) when countdown == 0

Add CourtCalibrationView.swift and ChallengeVideoView.swift to project.pbxproj with all required references.
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye" && xcodebuild build -project BadmintonEye/BadmintonEye.xcodeproj -scheme BadmintonEye -destination "platform=iOS Simulator,name=iPhone 16" -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CourtCalibrationView.swift exists with camera preview, 4-corner tap logic, green numbered dots, Confirm/Recalibrate buttons
    - ChallengeVideoView.swift exists with calibration check, Record Clip option, Select from Library option
    - LiveMatchView.swift contains Challenge button with 10-second countdown timer
    - LiveMatchView.swift shows Challenge button only when matchPhase == .inProgress
    - Project builds successfully
  </acceptance_criteria>
  <done>Challenge button appears in live match toolbar with 10s countdown after each rally. Tapping opens ChallengeVideoView sheet with camera recording or library selection. Court calibration via 4-corner tap saves to SwiftData.</done>
</task>

</tasks>

<verification>
- xcodebuild builds without errors
- LiveMatchView.swift contains "Challenge" button with countdown logic
- CalibrationProfile.swift exists as @Model with corner storage
- ChallengeVideoView.swift presents camera recording and photo library options
- CourtCalibrationView.swift implements 4-corner tap calibration
</verification>

<success_criteria>
- User can see Challenge button in live match view during active play
- Challenge button shows 10-second countdown after each rally
- Tapping Challenge opens video capture/selection sheet
- Court calibration view allows tapping 4 corners and saving per venue
- All files added to Xcode project and build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-hawk-eye-ai-and-premium/05-01-SUMMARY.md`
</output>
