---
phase: 04-cloud-sync-and-authentication
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, UX-01]

must_haves:
  truths:
    - "Settings tab shows Sign in with Apple button when signed out"
    - "Settings tab shows account info and iCloud status when signed in"
    - "App works fully without signing in"
    - "Live Activity appears on lock screen during active match"
    - "Dynamic Island shows compact score during active match"
---

<objective>
Human verification of all Phase 4 features: Apple Sign-In, CloudKit sync, local-only mode, and Live Activity

Purpose: Confirm that authentication, cloud sync, and Live Activity work correctly on a real device before marking the phase complete.
Output: Verified phase readiness
</objective>

<execution_context>
@/Users/milai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/milai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cloud-sync-and-authentication/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 4: Auth, CloudKit, and Live Activity</name>
  <files>none</files>
  <action>
  Present the verification checklist to the user for manual testing on a real device. This is a human-verify checkpoint -- no code changes.

  Plan 01 built: Apple Sign-In via AuthManager, CloudKit-toggled SwiftData modelContainer, Settings tab with sign-in card and account info display.
  Plan 02 built: Live Activity widget extension with lock screen expanded view (score, names, game dots, server) and Dynamic Island compact view (score only), integrated into LiveMatchViewModel lifecycle.

  Verification steps:

  **Local-only mode (AUTH-03):**
  1. Build and run on iPhone simulator
  2. Confirm app launches to Matches tab with full functionality
  3. Go to Settings tab -- confirm Sign in with Apple button is visible
  4. Start and complete a match without signing in -- confirm everything works

  **Sign in with Apple (AUTH-01):**
  5. On a real device (Sign in with Apple requires real device), tap Sign in with Apple in Settings tab
  6. Authenticate with Apple ID
  7. Confirm Settings tab now shows your name, email (if shared), and "iCloud Sync Active" status

  **CloudKit sync (AUTH-02):**
  8. After signing in, confirm existing matches and players still appear (auto-merge)
  9. If you have a second device signed into the same Apple ID, confirm data appears there too
  10. Note: CloudKit sync requires a real iCloud account and may take a few moments

  **Live Activity (UX-01):**
  11. On a real device (or simulator with Live Activity support), start a new match
  12. Lock the device -- confirm Live Activity shows on lock screen with score, player names, game dots, and server indicator
  13. Check Dynamic Island (if available) shows compact score
  14. Score a few points -- confirm Live Activity updates
  15. Complete the match -- confirm Live Activity ends (stays visible for ~5 minutes, then dismisses)
  </action>
  <verify>User confirms all 4 requirement areas pass manual testing</verify>
  <done>AUTH-01 (Apple Sign-In), AUTH-02 (CloudKit sync), AUTH-03 (local-only mode), and UX-01 (Live Activity) all verified on device</done>
</task>

</tasks>

<verification>
All Phase 4 requirements verified by human testing on device
</verification>

<success_criteria>
- AUTH-01: User can sign in with Apple ID
- AUTH-02: Match data and player profiles sync across devices via CloudKit
- AUTH-03: App works without signing in (local-only mode)
- UX-01: Active match score appears as Live Activity on lock screen and Dynamic Island
</success_criteria>

<output>
After completion, create `.planning/phases/04-cloud-sync-and-authentication/04-03-SUMMARY.md`
</output>
