---
phase: 03-match-data-and-player-profiles
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - BadmintonEye/BadmintonEye/Views/PlayerListView.swift
  - BadmintonEye/BadmintonEye/Views/PlayerProfileView.swift
  - BadmintonEye/BadmintonEye/Views/HeadToHeadView.swift
  - BadmintonEye/BadmintonEye/Views/PlayerPickerView.swift
  - BadmintonEye/BadmintonEye/Views/MatchSetupView.swift
  - BadmintonEye/BadmintonEye/App/BadmintonEyeApp.swift
autonomous: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "User can create a player profile with name and optional photo"
    - "User can browse an alphabetical player list with search, initials avatars, and W/L records"
    - "User can tap a player to see head-to-head record with big W-L number and match list"
    - "User can quick-select saved players when starting a match via tappable chips"
  artifacts:
    - path: "BadmintonEye/BadmintonEye/Views/PlayerListView.swift"
      provides: "Alphabetical player list with search and W/L"
      min_lines: 80
    - path: "BadmintonEye/BadmintonEye/Views/PlayerProfileView.swift"
      provides: "Create/edit player with name and photo"
      min_lines: 50
    - path: "BadmintonEye/BadmintonEye/Views/HeadToHeadView.swift"
      provides: "Head-to-head record screen"
      min_lines: 60
    - path: "BadmintonEye/BadmintonEye/Views/PlayerPickerView.swift"
      provides: "Quick-select chips for match setup"
      min_lines: 40
  key_links:
    - from: "BadmintonEye/BadmintonEye/Views/PlayerListView.swift"
      to: "HeadToHeadView"
      via: "NavigationLink from player row"
      pattern: "NavigationLink.*HeadToHeadView"
    - from: "BadmintonEye/BadmintonEye/Views/MatchSetupView.swift"
      to: "PlayerPickerView"
      via: "Sheet or inline picker for player selection"
      pattern: "PlayerPickerView"
    - from: "BadmintonEye/BadmintonEye/Views/HeadToHeadView.swift"
      to: "PersistedMatch"
      via: "Query filtering matches by player name"
      pattern: "Predicate.*playerAName.*playerBName"
---

<objective>
Add player profiles with create/edit, alphabetical list with search, head-to-head records, and quick-select player chips in match setup.

Purpose: Transforms the app from anonymous scoring to personalized match tracking. Players can build a history against specific opponents and quickly start matches with saved profiles.
Output: PlayerListView, PlayerProfileView, HeadToHeadView, PlayerPickerView, updated MatchSetupView
</objective>

<execution_context>
@/Users/milai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/milai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-match-data-and-player-profiles/03-CONTEXT.md
@.planning/phases/03-match-data-and-player-profiles/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From BadmintonEye/BadmintonEye/Models/Player.swift:
```swift
@Model
final class Player {
    var id: UUID = UUID()
    var name: String = ""
    var photoData: Data?
    var createdAt: Date = Date()
    init() {}
}
```

From BadmintonEye/BadmintonEye/Models/SwiftDataModels.swift:
```swift
@Model
final class PersistedMatch {
    var id: UUID = UUID()
    var format: String = "singles"
    var startedAt: Date = Date()
    var endedAt: Date?
    var stateJSON: Data?
    var isComplete: Bool = false
    var isAbandoned: Bool = false
    var playerAName: String?
    var playerBName: String?
    var playerA2Name: String?
    var playerB2Name: String?
    var winnerSide: String?  // "sideA" or "sideB"
    var game1ScoreA: Int = 0
    var game1ScoreB: Int = 0
    var game2ScoreA: Int?
    var game2ScoreB: Int?
    var game3ScoreA: Int?
    var game3ScoreB: Int?
}
```

From BadmintonEye/BadmintonEye/Views/MatchSetupView.swift:
```swift
struct MatchSetupView: View {
    @State private var selectedFormat: MatchFormat = .singles
    @State private var playerAName: String = ""
    @State private var playerBName: String = ""
    @State private var playerA2Name: String = ""
    @State private var playerB2Name: String = ""
    // ... Form with TextFields for player names, Start Match button
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Player list, profile create/edit, and head-to-head views</name>
  <read_first>
    - BadmintonEye/BadmintonEye/Models/Player.swift
    - BadmintonEye/BadmintonEye/Models/SwiftDataModels.swift
    - BadmintonEye/BadmintonEye/App/BadmintonEyeApp.swift
  </read_first>
  <files>
    BadmintonEye/BadmintonEye/Views/PlayerListView.swift,
    BadmintonEye/BadmintonEye/Views/PlayerProfileView.swift,
    BadmintonEye/BadmintonEye/Views/HeadToHeadView.swift,
    BadmintonEye/BadmintonEye/App/BadmintonEyeApp.swift
  </files>
  <action>
    1. Create `BadmintonEye/BadmintonEye/Views/PlayerListView.swift`:
       - `@Query(sort: \Player.name)` to fetch all players alphabetically
       - `.searchable(text:)` modifier for filtering by name (use `players.filter { $0.name.localizedCaseInsensitiveContains(searchText) }` when searchText is non-empty)
       - Each row: left side shows initials avatar (circle with first letter of name, colored background using hash of name to pick from a palette of 8 colors: .blue, .green, .orange, .purple, .pink, .teal, .indigo, .mint), or thumbnail photo if `photoData` is non-nil (decoded as UIImage). Right side shows W-L record computed from PersistedMatch query.
       - W/L computation: for each player, count matches where `(playerAName == player.name && winnerSide == "sideA") || (playerBName == player.name && winnerSide == "sideB")` as wins. Similar for losses. Also check playerA2Name/playerB2Name for doubles. Use a helper function, not inline query, to keep the view clean.
       - NavigationLink destination: `HeadToHeadView(player:)` (shows record against the current user -- but since there's no "current user" concept yet, show the player's overall stats and list of their matches)
       - Toolbar "+" button presents `PlayerProfileView(player: nil)` as `.sheet` for creating new player
       - Tap existing player row navigates to HeadToHeadView; long-press or swipe shows edit option presenting PlayerProfileView(player: existingPlayer)
       - `.navigationTitle("Players")`

    2. Create `BadmintonEye/BadmintonEye/Views/PlayerProfileView.swift`:
       - Takes optional `Player?` parameter (nil = create, non-nil = edit)
       - Form with: name TextField (required, disable Save if empty), photo section with PhotosPicker (import PhotosUI) for selecting from library
       - When photo selected, resize to 200x200 max thumbnail and store as JPEG data (compressionQuality 0.7) in `player.photoData`
       - Save button: if creating, insert new Player into modelContext; if editing, update existing. Dismiss sheet on save.
       - Delete button (only in edit mode): delete player from modelContext with confirmation alert
       - `.navigationTitle(player == nil ? "New Player" : "Edit Player")`
       - Use `@Environment(\.dismiss)` and `@Environment(\.modelContext)`

    3. Create `BadmintonEye/BadmintonEye/Views/HeadToHeadView.swift`:
       - Takes a `Player` parameter
       - Top section: large initials avatar or photo, player name
       - Overall record: big "W - L" display (e.g., "15 - 8") with win rate percentage below
       - Match history against ALL opponents: `@Query` filtering PersistedMatch where playerAName/playerBName/playerA2Name/playerB2Name matches player.name, sorted by startedAt descending
       - Each match row shows: opponent name, date, score summary, win/loss badge
       - If user taps a specific opponent in the list, could filter -- but for MVP, just show all matches for this player
       - Per the CONTEXT.md decision: "dedicated screen from player profile with big W-L number, match list against that opponent, win rate percentage" -- since we don't have opponent context when navigating from player list, show overall stats. The head-to-head filtering (against a SPECIFIC opponent) can be triggered by tapping an opponent name in the match list.
       - Add a `headToHeadAgainst(opponent: String)` section that groups matches by opponent and shows per-opponent W-L. Make each opponent row tappable to filter the match list.

    4. Update `BadmintonEyeApp.swift` ContentView:
       - Add a TabView for iPhone (or keep NavigationStack and add Players as a tab):
         Actually, per iOS convention and simplicity, add a "Players" tab to a TabView wrapper:
         - Tab 1: Match History (MatchHistoryView) with "sportscourt" icon
         - Tab 2: Players (PlayerListView) with "person.2" icon
         Keep the iPad NavigationSplitView sidebar with both sections (Matches + Players).
       - Ensure crash recovery still works: check inProgressMatches on appear, present LiveMatchView if needed.
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye" && wc -l BadmintonEye/BadmintonEye/Views/PlayerListView.swift BadmintonEye/BadmintonEye/Views/PlayerProfileView.swift BadmintonEye/BadmintonEye/Views/HeadToHeadView.swift && grep -c "searchable\|@Query\|NavigationLink" BadmintonEye/BadmintonEye/Views/PlayerListView.swift && grep -c "PhotosPicker\|photoData\|TextField" BadmintonEye/BadmintonEye/Views/PlayerProfileView.swift && grep "TabView\|PlayerListView" BadmintonEye/BadmintonEye/App/BadmintonEyeApp.swift</automated>
  </verify>
  <acceptance_criteria>
    - PlayerListView.swift has @Query sorted by name, .searchable modifier, initials avatar with color from name hash, W/L record per player, "+" toolbar button for new player, NavigationLink to HeadToHeadView
    - PlayerProfileView.swift has Form with name TextField, PhotosPicker for optional photo, save/delete buttons, works for both create and edit
    - HeadToHeadView.swift shows player avatar, big W-L record, win rate percentage, match list, per-opponent breakdown
    - BadmintonEyeApp.swift has TabView with Matches and Players tabs (iPhone) or sidebar sections (iPad)
  </acceptance_criteria>
  <done>Users can create/edit player profiles, browse alphabetical player list with search and W/L stats, view head-to-head records</done>
</task>

<task type="auto">
  <name>Task 2: Player quick-select integration in match setup</name>
  <read_first>
    - BadmintonEye/BadmintonEye/Views/MatchSetupView.swift
    - BadmintonEye/BadmintonEye/Models/Player.swift
  </read_first>
  <files>
    BadmintonEye/BadmintonEye/Views/PlayerPickerView.swift,
    BadmintonEye/BadmintonEye/Views/MatchSetupView.swift
  </files>
  <action>
    1. Create `BadmintonEye/BadmintonEye/Views/PlayerPickerView.swift`:
       - Reusable view that lets user pick a Player from saved profiles
       - Parameters: `@Binding var selectedName: String`, `label: String` (e.g., "Player 1"), `excludeNames: [String]` (to prevent selecting same player twice)
       - Top section: "Recent Opponents" -- horizontal ScrollView of tappable chips showing the 5 most recent unique player names from PersistedMatch (query by startedAt desc, extract unique names). Each chip: rounded rectangle with player name, tappable to set selectedName.
       - Below: full player list from `@Query(sort: \Player.name)`, filtered to exclude excludeNames, each row shows initials avatar + name, tappable to set selectedName
       - Search bar for filtering the full list
       - When a player is selected (chip or list row), set selectedName to player.name
       - Also allow manual text entry: keep the existing TextField visible above the picker as fallback for players not yet in profiles

    2. Update `BadmintonEye/BadmintonEye/Views/MatchSetupView.swift`:
       - Replace each plain TextField for player names with a composite:
         - TextField still present for manual entry
         - Below each TextField, add PlayerPickerView as an expandable/collapsible section (use DisclosureGroup or a "Select Player" button that shows a sheet)
         - Better UX: use a sheet approach. Add a small "person.circle" button next to each TextField. Tapping it opens a sheet with PlayerPickerView. When player selected, sheet dismisses and name fills into the TextField.
       - For doubles: show picker buttons for all 4 player fields
       - Pass `excludeNames` containing already-selected player names to prevent duplicates
       - After match starts, if the entered name matches no existing Player, auto-create a Player record for that name (fire-and-forget insert into modelContext). This way the player list grows organically.
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye" && wc -l BadmintonEye/BadmintonEye/Views/PlayerPickerView.swift && grep -c "ScrollView\|@Query\|selectedName\|chip" BadmintonEye/BadmintonEye/Views/PlayerPickerView.swift && grep -c "PlayerPickerView\|person.circle\|sheet" BadmintonEye/BadmintonEye/Views/MatchSetupView.swift</automated>
  </verify>
  <acceptance_criteria>
    - PlayerPickerView.swift exists with recent opponent chips (horizontal ScrollView), full player list with search, @Binding for selectedName, excludeNames parameter
    - MatchSetupView.swift has player picker button next to each name TextField, opens sheet with PlayerPickerView, selected name fills TextField
    - Selecting a player from chips or list sets the name binding and dismisses
    - Starting a match auto-creates Player records for names not already in the database
    - PlayerPickerView has 40+ lines
  </acceptance_criteria>
  <done>Users can quick-select saved players via tappable chips or searchable list when starting a match, and new player profiles are auto-created</done>
</task>

</tasks>

<verification>
- Build the project: `xcodebuild build` should succeed
- All 4 new view files exist and have substantial content
- MatchSetupView integrates PlayerPickerView
- TabView or equivalent navigation provides access to both match history and player list
</verification>

<success_criteria>
- Player profiles with name, optional photo, CRUD operations
- Alphabetical player list with search, initials avatars, W/L records
- Head-to-head view with big W-L, win rate, match list, per-opponent breakdown
- Quick-select player chips in match setup from recent opponents
- Auto-creation of Player records when starting matches with new names
</success_criteria>

<output>
After completion, create `.planning/phases/03-match-data-and-player-profiles/03-02-SUMMARY.md`
</output>
