---
phase: 07-training-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/training/train.py
  - scripts/training/requirements.txt
  - scripts/training/README.md
  - scripts/training/ANNOTATION_GUIDE.md
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-04
must_haves:
  truths:
    - "Running train.py on an annotated YOLO-format dataset produces a .mlmodel file"
    - "train.py accepts CLI args for dataset path, epochs, image size, and output path"
    - "Annotation guide documents shuttlecock bounding box labeling including motion blur"
    - "README specifies 2000+ images, diverse courts/lighting, and step-by-step instructions"
  artifacts:
    - path: "scripts/training/train.py"
      provides: "YOLO nano training and CoreML export script"
      min_lines: 80
    - path: "scripts/training/requirements.txt"
      provides: "Python dependencies"
      contains: "ultralytics"
    - path: "scripts/training/README.md"
      provides: "Training workflow documentation"
      contains: "2,000"
    - path: "scripts/training/ANNOTATION_GUIDE.md"
      provides: "Shuttlecock annotation instructions"
      contains: "motion blur"
  key_links:
    - from: "scripts/training/train.py"
      to: "ultralytics"
      via: "YOLO import and export"
      pattern: "from ultralytics import YOLO"
    - from: "scripts/training/train.py"
      to: ".mlmodel output"
      via: "CoreML export with NMS"
      pattern: "export.*coreml.*nms"
---

<objective>
Create the Python-based YOLO training workflow: training script with CoreML export, annotation guide for shuttlecock labeling, and README documenting the full dataset-to-model pipeline.

Purpose: Enables the developer to train a production YOLO nano model from annotated badminton footage and export it as CoreML for integration in Phase 9.
Output: scripts/training/ directory with train.py, requirements.txt, README.md, ANNOTATION_GUIDE.md
</objective>

<execution_context>
@/Users/milai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/milai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create training script and requirements</name>
  <files>scripts/training/train.py, scripts/training/requirements.txt</files>
  <action>
    read_first: .planning/phases/07-training-pipeline/07-CONTEXT.md (confirm Ultralytics YOLO nano, CoreML export with NMS)

    A. Create scripts/training/requirements.txt:
       ```
       ultralytics>=8.1.0
       coremltools>=7.0
       torch>=2.0.0
       ```

    B. Create scripts/training/train.py:
       - Shebang line: `#!/usr/bin/env python3`
       - argparse CLI with these arguments:
         - `--data` (required): path to YOLO-format dataset.yaml
         - `--epochs` (default 100): training epochs
         - `--imgsz` (default 640): input image size
         - `--batch` (default 16): batch size
         - `--output` (default "runs/shuttle_detect"): output directory
         - `--model` (default "yolo11n.pt"): base YOLO model (nano)
         - `--name` (default "shuttle_v1"): run name
       - Main workflow:
         1. Print banner with config summary
         2. Load base model: `model = YOLO(args.model)`
         3. Train: `model.train(data=args.data, epochs=args.epochs, imgsz=args.imgsz, batch=args.batch, project=args.output, name=args.name, exist_ok=True)`
         4. Validate: `metrics = model.val()`
         5. Print mAP50 and mAP50-95 from metrics
         6. Export to CoreML with NMS: `model.export(format='coreml', nms=True)`
         7. Print path to exported .mlmodel file
         8. Print reminder: "Copy the .mlmodel file into BadmintonEye/BadmintonEye/Models/ and add to Xcode project"
       - Wrap main in `if __name__ == "__main__":`
       - Include docstring at module level explaining purpose

    acceptance_criteria: train.py contains `from ultralytics import YOLO`, argparse with --data/--epochs/--imgsz/--batch/--output/--model/--name, `model.train(`, `model.export(format='coreml', nms=True)`, `if __name__`. requirements.txt contains ultralytics, coremltools, torch.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/training/train.py').read()); print('SYNTAX OK')" 2>&1 && grep -c "ultralytics" scripts/training/requirements.txt</automated>
  </verify>
  <done>train.py parses without syntax errors. requirements.txt lists all dependencies. Script accepts CLI arguments and runs Ultralytics train + CoreML export.</done>
</task>

<task type="auto">
  <name>Task 2: Create annotation guide and training README</name>
  <files>scripts/training/ANNOTATION_GUIDE.md, scripts/training/README.md</files>
  <action>
    read_first: .planning/REQUIREMENTS.md (lines 35-38 — TRAIN-02 and TRAIN-04 requirements)

    A. Create scripts/training/ANNOTATION_GUIDE.md:
       Sections:
       1. Overview — what we are annotating (shuttlecocks in badminton match footage) and why (YOLO nano object detection)
       2. Tool Setup — recommend CVAT or Label Studio (free), YOLO format export (class_id center_x center_y width height, normalized 0-1)
       3. Labeling Rules:
          - Single class: `shuttlecock` (class_id = 0)
          - Bounding box should tightly enclose the shuttlecock including feathers
          - For motion blur: draw box around the full blur streak (not just the cork)
          - If shuttlecock is partially occluded (by net, player), still annotate visible portion
          - Skip frames where shuttlecock is completely invisible
          - One annotation per shuttlecock per frame (there is only one shuttle in play)
       4. Motion Blur Specifics — examples of how blur elongates the shuttle, box should capture full extent, label even when blurred
       5. Dataset Directory Structure — show YOLO format:
          ```
          dataset/
            images/
              train/
              val/
            labels/
              train/
              val/
            dataset.yaml
          ```
       6. dataset.yaml example:
          ```yaml
          path: ./dataset
          train: images/train
          val: images/val
          names:
            0: shuttlecock
          ```
       7. Quality Checklist — minimum 2,000 images, 70/30 train/val split, diverse courts (indoor/outdoor), diverse lighting, various camera angles, include rally and serve frames

    B. Create scripts/training/README.md:
       Sections:
       1. Title: "Badminton Eye - Shuttle Detection Training Pipeline"
       2. Overview — trains YOLOv11 nano for shuttlecock detection, exports CoreML for on-device inference
       3. Prerequisites — Python 3.10+, pip, GPU recommended (CUDA for NVIDIA, MPS for Apple Silicon)
       4. Setup:
          ```bash
          cd scripts/training
          pip install -r requirements.txt
          ```
       5. Dataset Requirements:
          - Minimum 2,000 annotated images (3,000+ recommended)
          - Diverse courts: indoor wooden, outdoor concrete, various colors
          - Diverse lighting: natural daylight, fluorescent, mixed
          - Various camera angles: side-court, behind-court, elevated
          - Include both rally shots and serves
          - Motion blur frames mandatory (at least 20% of dataset)
          - YOLO format (see ANNOTATION_GUIDE.md)
       6. Training:
          ```bash
          python train.py --data path/to/dataset.yaml --epochs 100 --imgsz 640
          ```
          Explain each argument. Note: first run downloads yolo11n.pt base weights automatically.
       7. Expected Output — .mlmodel file location, expected mAP50 target (>0.7 for production use)
       8. Integration — copy .mlmodel to BadmintonEye/BadmintonEye/Models/, add to Xcode project, create CoreMLShuttleDetector conforming to ShuttleDetecting protocol (Phase 9)
       9. Troubleshooting — common issues (CUDA OOM: reduce batch size, MPS: set PYTORCH_MPS_HIGH_WATERMARK_RATIO=0.0, low mAP: check annotation quality and dataset diversity)

    acceptance_criteria: ANNOTATION_GUIDE.md contains "motion blur", "bounding box", "shuttlecock", "YOLO format", "dataset.yaml". README.md contains "2,000", "CoreML", "pip install", "train.py", "ShuttleDetecting".
  </action>
  <verify>
    <automated>grep -c "motion blur" scripts/training/ANNOTATION_GUIDE.md && grep -c "2,000" scripts/training/README.md && grep -c "ShuttleDetecting" scripts/training/README.md</automated>
  </verify>
  <done>Annotation guide covers shuttlecock labeling with motion blur handling. README documents full pipeline from dataset requirements through training to CoreML integration. Both reference 2,000+ image requirement.</done>
</task>

</tasks>

<verification>
1. train.py has valid Python syntax
2. train.py uses Ultralytics YOLO with CoreML export and NMS
3. requirements.txt lists ultralytics, coremltools, torch
4. ANNOTATION_GUIDE.md covers bounding box labeling and motion blur
5. README.md specifies 2,000+ images, diverse courts/lighting, and step-by-step workflow
</verification>

<success_criteria>
- train.py accepts dataset path and training parameters via CLI, trains YOLO nano, exports CoreML with NMS
- requirements.txt pins minimum versions for reproducibility
- ANNOTATION_GUIDE.md gives clear labeling instructions including motion blur edge cases
- README.md documents the full workflow from dataset collection through model integration
</success_criteria>

<output>
After completion, create `.planning/phases/07-training-pipeline/07-02-SUMMARY.md`
</output>
