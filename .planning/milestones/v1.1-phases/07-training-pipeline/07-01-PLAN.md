---
phase: 07-training-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BadmintonEye/BadmintonEye/Services/ShuttleDetecting.swift
  - BadmintonEye/BadmintonEye/Services/PlaceholderShuttleDetector.swift
  - BadmintonEye/BadmintonEye/Services/HawkEyePipeline.swift
autonomous: true
requirements:
  - TRAIN-03
must_haves:
  truths:
    - "ShuttleDetecting protocol defines async detect(in:) -> [ShuttleObservation]"
    - "PlaceholderShuttleDetector conforms to ShuttleDetecting and produces the same output as the current generatePlaceholderPositions"
    - "HawkEyePipeline uses a ShuttleDetecting instance instead of calling generatePlaceholderPositions directly"
    - "Swapping detector implementation requires changing only the injected ShuttleDetecting conformance"
  artifacts:
    - path: "BadmintonEye/BadmintonEye/Services/ShuttleDetecting.swift"
      provides: "Protocol contract for shuttle detection"
      exports: ["ShuttleDetecting", "ShuttleObservation"]
    - path: "BadmintonEye/BadmintonEye/Services/PlaceholderShuttleDetector.swift"
      provides: "Placeholder implementation preserving current behavior"
      exports: ["PlaceholderShuttleDetector"]
    - path: "BadmintonEye/BadmintonEye/Services/HawkEyePipeline.swift"
      provides: "Refactored pipeline using injected detector"
  key_links:
    - from: "HawkEyePipeline.swift"
      to: "ShuttleDetecting.swift"
      via: "Protocol-typed property injection"
      pattern: "var detector: ShuttleDetecting"
    - from: "PlaceholderShuttleDetector.swift"
      to: "ShuttleDetecting.swift"
      via: "Protocol conformance"
      pattern: "class PlaceholderShuttleDetector.*ShuttleDetecting"
---

<objective>
Define the ShuttleDetecting protocol contract and refactor HawkEyePipeline to use protocol-based injection, so that the trained YOLO model (Phase 9) can be swapped in without pipeline changes.

Purpose: Establishes the abstraction boundary between detection and trajectory analysis. This is the integration point the training pipeline (Plan 02) exports to.
Output: ShuttleDetecting.swift protocol, PlaceholderShuttleDetector.swift, refactored HawkEyePipeline.swift
</objective>

<execution_context>
@/Users/milai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/milai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@BadmintonEye/BadmintonEye/Services/HawkEyePipeline.swift
@BadmintonEye/BadmintonEye/Services/TrajectoryCalculator.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define ShuttleDetecting protocol and ShuttleObservation model</name>
  <files>BadmintonEye/BadmintonEye/Services/ShuttleDetecting.swift</files>
  <action>
    read_first: BadmintonEye/BadmintonEye/Services/HawkEyePipeline.swift (lines 82-165 — understand generatePlaceholderPositions input/output)

    Create ShuttleDetecting.swift with:

    1. ShuttleObservation struct:
       - `position: CGPoint` (image-space coordinate)
       - `confidence: Float` (0.0-1.0, placeholder always returns 0.9)
       - `frameIndex: Int` (which frame this detection came from)

    2. ShuttleDetecting protocol:
       - `func detect(in pixelBuffer: CVPixelBuffer) async throws -> [ShuttleObservation]`
       - `func detect(imageSize: CGSize, frameCount: Int) async throws -> [ShuttleObservation]` — convenience for placeholder mode (no real frames)
       - `var modelName: String { get }` — human-readable name for logging

    Mark both as public. Import CoreVideo for CVPixelBuffer, Foundation, and CoreGraphics.

    acceptance_criteria: File contains `protocol ShuttleDetecting`, `struct ShuttleObservation`, three members in the protocol, CVPixelBuffer parameter in detect method.
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye/BadmintonEye" && xcodebuild build -scheme BadmintonEye -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>ShuttleDetecting.swift exists with protocol and observation type. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Extract PlaceholderShuttleDetector and refactor HawkEyePipeline</name>
  <files>BadmintonEye/BadmintonEye/Services/PlaceholderShuttleDetector.swift, BadmintonEye/BadmintonEye/Services/HawkEyePipeline.swift</files>
  <action>
    read_first: BadmintonEye/BadmintonEye/Services/HawkEyePipeline.swift, BadmintonEye/BadmintonEye/Services/ShuttleDetecting.swift (just created in Task 1)

    A. Create PlaceholderShuttleDetector.swift:
       - `final class PlaceholderShuttleDetector: ShuttleDetecting`
       - `var modelName: String { "Placeholder v1.0" }`
       - Move `generatePlaceholderPositions(count:imageSize:)` logic from HawkEyePipeline into `detect(imageSize:frameCount:)` implementation
       - Map each generated CGPoint into a ShuttleObservation with confidence 0.9 and sequential frameIndex
       - The `detect(in: CVPixelBuffer)` method throws a descriptive error ("Placeholder detector does not support real frame analysis") — it exists only to satisfy the protocol
       - Mark class as @unchecked Sendable (same pattern as HawkEyePipeline)

    B. Refactor HawkEyePipeline.swift:
       - Add stored property: `private let detector: ShuttleDetecting`
       - Add init: `init(detector: ShuttleDetecting = PlaceholderShuttleDetector())`
       - In analyze(), replace the direct call to `generatePlaceholderPositions` (lines 84-88) with: `let observations = try await detector.detect(imageSize: imageSize, frameCount: detectionCount)`
       - Map observations to CGPoints: `let simulatedImagePositions = observations.map { $0.position }`
       - Delete the private `generatePlaceholderPositions` method entirely
       - Keep all other pipeline logic unchanged (homography, trajectory, confidence)

    acceptance_criteria: HawkEyePipeline has `detector: ShuttleDetecting` property. `generatePlaceholderPositions` method is gone from HawkEyePipeline. PlaceholderShuttleDetector conforms to ShuttleDetecting.
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye/BadmintonEye" && xcodebuild build -scheme BadmintonEye -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>HawkEyePipeline uses injected ShuttleDetecting. Placeholder extracted. Build succeeds. Swapping detector requires only changing the init parameter.</done>
</task>

</tasks>

<verification>
1. Project builds without errors
2. ShuttleDetecting protocol exists with detect(in:) and detect(imageSize:frameCount:) methods
3. HawkEyePipeline.swift no longer contains generatePlaceholderPositions
4. HawkEyePipeline.swift contains `detector: ShuttleDetecting`
5. PlaceholderShuttleDetector conforms to ShuttleDetecting
</verification>

<success_criteria>
- ShuttleDetecting protocol defines the contract for shuttle detection (CVPixelBuffer-based and convenience)
- PlaceholderShuttleDetector preserves identical behavior to the old generatePlaceholderPositions
- HawkEyePipeline accepts any ShuttleDetecting conformance via init injection
- Full project compiles and existing Hawk Eye challenge flow works unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07-training-pipeline/07-01-SUMMARY.md`
</output>
