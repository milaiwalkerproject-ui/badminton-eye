---
phase: 06-match-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - BadmintonEye/BadmintonEye/Views/WinRateTrendChart.swift
  - BadmintonEye/BadmintonEye/Views/ScoringPatternsChart.swift
  - BadmintonEye/BadmintonEye/Views/StatsView.swift
autonomous: false
requirements:
  - STAT-02
  - STAT-03

must_haves:
  truths:
    - "User can view a line chart of win rate over last 10, 20, or all matches with a segmented control toggle"
    - "User can view a grouped bar chart of average points scored and conceded per game (1, 2, 3)"
    - "Charts render from existing PersistedMatch data with no new models"
    - "Segmented control defaults to Last 10 and updates chart immediately on toggle"
  artifacts:
    - path: "BadmintonEye/BadmintonEye/Views/WinRateTrendChart.swift"
      provides: "Line chart with segmented range picker"
      contains: "struct WinRateTrendChart"
    - path: "BadmintonEye/BadmintonEye/Views/ScoringPatternsChart.swift"
      provides: "Grouped bar chart for per-game averages"
      contains: "struct ScoringPatternsChart"
    - path: "BadmintonEye/BadmintonEye/Views/StatsView.swift"
      provides: "Dashboard integrating summary card + both charts"
      contains: "WinRateTrendChart"
  key_links:
    - from: "BadmintonEye/BadmintonEye/Views/WinRateTrendChart.swift"
      to: "MatchStatsViewModel.winRateOverLast"
      via: "function call with selected range"
      pattern: "winRateOverLast"
    - from: "BadmintonEye/BadmintonEye/Views/ScoringPatternsChart.swift"
      to: "MatchStatsViewModel.perGameAverages"
      via: "function call"
      pattern: "perGameAverages"
    - from: "BadmintonEye/BadmintonEye/Views/StatsView.swift"
      to: "WinRateTrendChart and ScoringPatternsChart"
      via: "embedded in ScrollView"
      pattern: "WinRateTrendChart|ScoringPatternsChart"
---

<objective>
Add Swift Charts visualizations to the stats dashboard: a win rate trend line chart with configurable time range (Last 10/20/All) and a grouped bar chart showing per-game scoring patterns.

Purpose: Gives competitive players visual insight into performance trends and game-by-game patterns so they can identify strengths and weaknesses.
Output: WinRateTrendChart.swift, ScoringPatternsChart.swift, updated StatsView.swift replacing placeholders with real charts.
</objective>

<execution_context>
@/Users/milai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/milai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-match-analytics/06-CONTEXT.md
@.planning/phases/06-match-analytics/06-01-SUMMARY.md

@BadmintonEye/BadmintonEye/ViewModels/MatchStatsViewModel.swift
@BadmintonEye/BadmintonEye/Views/StatsView.swift

<interfaces>
<!-- From Plan 01 MatchStatsViewModel -- these are the data sources for charts -->

From BadmintonEye/BadmintonEye/ViewModels/MatchStatsViewModel.swift:
```swift
@Observable class MatchStatsViewModel {
    // Plan 02 consumes these:
    func winRateOverLast(_ n: Int) -> [(index: Int, rate: Double)]
    // Returns rolling cumulative win rate for charting.
    // n=10 for last 10, n=20 for last 20, n=0 for all matches.
    // Each tuple: (match index from 1..n, cumulative win rate 0-100 at that point)

    func perGameAverages() -> [(game: Int, avgScored: Double, avgConceded: Double)]
    // Returns averages for games 1, 2, 3.
    // game: 1/2/3, avgScored: user's avg points, avgConceded: opponent's avg points.
    // Only includes games with data (game 3 omitted if no 3-game matches).

    var hasEnoughData: Bool  // true if >= 3 completed matches
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WinRateTrendChart and ScoringPatternsChart views</name>
  <files>BadmintonEye/BadmintonEye/Views/WinRateTrendChart.swift, BadmintonEye/BadmintonEye/Views/ScoringPatternsChart.swift</files>
  <action>
    read_first: BadmintonEye/BadmintonEye/ViewModels/MatchStatsViewModel.swift (for winRateOverLast and perGameAverages signatures), BadmintonEye/BadmintonEye/Views/StatsView.swift (for current layout)

    **WinRateTrendChart.swift:**

    Create a SwiftUI view that takes a MatchStatsViewModel binding/reference and renders a win rate trend line chart.

    - Import Charts framework
    - Enum for range selection: `enum TrendRange: String, CaseIterable { case last10 = "Last 10", last20 = "Last 20", all = "All" }`
    - @State var selectedRange: TrendRange = .last10
    - Segmented Picker bound to selectedRange, placed above the chart
    - Compute data: call viewModel.winRateOverLast(n) where n = 10/20/0 based on selectedRange
    - Chart: LineMark with x = match index, y = win rate percentage
      - AreaMark underneath with low opacity for visual fill
      - Y-axis from 0 to 100 (percentage)
      - X-axis shows match numbers
      - Annotation: show current (latest) win rate value as a label on the last point
    - Chart height: 200pt
    - Wrap in a card-style container (RoundedRectangle background, padding, shadow matching summary card style)
    - Section header: "Performance Trend"

    **ScoringPatternsChart.swift:**

    Create a SwiftUI view that takes a MatchStatsViewModel reference and renders per-game scoring patterns.

    - Import Charts framework
    - Define a struct for chart data: `struct GameScoreData: Identifiable { let id = UUID(); let game: String; let type: String; let value: Double }`
      - type is "Scored" or "Conceded"
    - Get data from viewModel.perGameAverages(), transform into GameScoreData array
    - Chart: BarMark with x = game label ("Game 1", "Game 2", "Game 3"), y = average points
      - foregroundStyle by type (Scored vs Conceded) -- use green for scored, red for conceded
      - position by type for grouped bars (side-by-side, not stacked)
    - Chart height: 180pt
    - Wrap in card-style container matching WinRateTrendChart
    - Section header: "Scoring Patterns"
    - If perGameAverages returns empty, show a "Not enough data" message instead of chart

    acceptance_criteria:
    - WinRateTrendChart.swift contains `import Charts` and `struct WinRateTrendChart: View`
    - WinRateTrendChart has Picker with TrendRange enum (Last 10, Last 20, All)
    - WinRateTrendChart uses LineMark
    - ScoringPatternsChart.swift contains `import Charts` and `struct ScoringPatternsChart: View`
    - ScoringPatternsChart uses BarMark with foregroundStyle by type
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye" && grep -q "import Charts" BadmintonEye/BadmintonEye/Views/WinRateTrendChart.swift && grep -q "LineMark" BadmintonEye/BadmintonEye/Views/WinRateTrendChart.swift && grep -q "import Charts" BadmintonEye/BadmintonEye/Views/ScoringPatternsChart.swift && grep -q "BarMark" BadmintonEye/BadmintonEye/Views/ScoringPatternsChart.swift && grep -q "TrendRange" BadmintonEye/BadmintonEye/Views/WinRateTrendChart.swift && echo "PASS"</automated>
  </verify>
  <done>Both chart views created: WinRateTrendChart with segmented range picker and line chart, ScoringPatternsChart with grouped bar chart for per-game averages.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate charts into StatsView replacing placeholders</name>
  <files>BadmintonEye/BadmintonEye/Views/StatsView.swift</files>
  <action>
    read_first: BadmintonEye/BadmintonEye/Views/StatsView.swift (current layout with placeholders), BadmintonEye/BadmintonEye/Views/WinRateTrendChart.swift (just created), BadmintonEye/BadmintonEye/Views/ScoringPatternsChart.swift (just created)

    Update StatsView.swift:
    1. Replace the "Performance Trend" placeholder section with WinRateTrendChart(viewModel: viewModel)
    2. Replace the "Scoring Patterns" placeholder section with ScoringPatternsChart(viewModel: viewModel)
    3. Only show chart sections when viewModel.hasEnoughData is true (keep empty state behavior from Plan 01)
    4. Ensure ScrollView contains: summary card -> WinRateTrendChart -> ScoringPatternsChart in vertical stack with consistent spacing (16pt between cards)

    Do NOT change the summary card or empty state -- only replace the two placeholder sections.

    acceptance_criteria:
    - StatsView.swift contains WinRateTrendChart reference
    - StatsView.swift contains ScoringPatternsChart reference
    - No "Coming soon" placeholder text remains
    - hasEnoughData guard still controls chart visibility
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye" && grep -q "WinRateTrendChart" BadmintonEye/BadmintonEye/Views/StatsView.swift && grep -q "ScoringPatternsChart" BadmintonEye/BadmintonEye/Views/StatsView.swift && ! grep -q "Coming soon" BadmintonEye/BadmintonEye/Views/StatsView.swift && echo "PASS"</automated>
  </verify>
  <done>StatsView dashboard complete with summary card, win rate trend chart with segmented range control, and per-game scoring patterns bar chart.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify stats dashboard in simulator</name>
  <files>none</files>
  <action>
    Human visual verification of the complete stats dashboard. Claude builds and launches the simulator before this checkpoint.

    What was built: Complete match analytics dashboard with summary card (W/L/streak), win rate trend line chart with Last 10/20/All segmented control, and per-game scoring patterns grouped bar chart. Accessible from Stats tab on iPhone and Stats sidebar section on iPad.

    How to verify:
    1. Open the app in iPhone simulator
    2. Verify "Stats" tab appears as the third tab (between Players and Settings) with chart.bar icon
    3. If fewer than 3 matches exist: verify empty state shows "Play more matches to unlock analytics"
    4. If 3+ matches exist:
       a. Summary card shows correct W-L record, win rate percentage, and win streak
       b. Win rate trend chart shows line chart; toggle Last 10 / Last 20 / All and verify chart updates
       c. Scoring patterns shows grouped bars for Game 1/2/3 with scored (green) and conceded (red) bars
    5. Switch to iPad simulator and verify Stats appears in sidebar navigation

    Resume signal: Type "approved" or describe any visual/functional issues
  </action>
  <verify>
    <automated>echo "Human verification checkpoint -- no automated check"</automated>
  </verify>
  <done>User confirms stats dashboard displays correctly on both iPhone and iPad with all charts rendering properly.</done>
</task>

</tasks>

<verification>
1. Build succeeds: `xcodebuild build -scheme BadmintonEye -destination 'platform=iOS Simulator,name=iPhone 16' -quiet`
2. Stats tab visible and functional on both iPhone and iPad layouts
3. Charts render with real data when 3+ completed matches exist
4. Segmented control toggles trend chart range
5. Empty state displayed when insufficient data
</verification>

<success_criteria>
- Win rate trend chart displays line chart with segmented control (Last 10 / Last 20 / All)
- Scoring patterns chart displays grouped bar chart for games 1/2/3
- Charts use Swift Charts framework (LineMark, BarMark)
- Both charts integrated into StatsView scrollable layout
- Visual verification confirms charts render correctly in simulator
</success_criteria>

<output>
After completion, create `.planning/phases/06-match-analytics/06-02-SUMMARY.md`
</output>
