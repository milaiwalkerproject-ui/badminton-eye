---
phase: 04-cloud-sync-and-authentication
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - BadmintonEye/BadmintonEye/ViewModels/LiveMatchViewModel.swift
  - BadmintonEye/BadmintonEyeLiveActivity/BadmintonEyeLiveActivityBundle.swift
  - BadmintonEye/BadmintonEyeLiveActivity/BadmintonEyeLiveActivityLiveActivity.swift
  - BadmintonEye/BadmintonEyeLiveActivity/MatchActivityAttributes.swift
  - BadmintonEye/BadmintonEye/Info.plist
  - BadmintonEye/BadmintonEye.xcodeproj/project.pbxproj
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "Active match score appears as a Live Activity on iPhone lock screen"
    - "Dynamic Island compact view shows current score"
    - "Live Activity auto-starts when match begins"
    - "Live Activity auto-ends when match completes or is abandoned"
    - "Lock screen expanded view shows player names, score, game dots, and server"
  artifacts:
    - path: "BadmintonEye/BadmintonEyeLiveActivity/MatchActivityAttributes.swift"
      provides: "ActivityAttributes defining match Live Activity data model"
      exports: ["MatchActivityAttributes"]
    - path: "BadmintonEye/BadmintonEyeLiveActivity/BadmintonEyeLiveActivityLiveActivity.swift"
      provides: "Live Activity UI for lock screen and Dynamic Island"
      min_lines: 60
    - path: "BadmintonEye/BadmintonEye/ViewModels/LiveMatchViewModel.swift"
      provides: "Live Activity start/update/end integration in match lifecycle"
      contains: "Activity<MatchActivityAttributes>"
  key_links:
    - from: "LiveMatchViewModel.swift"
      to: "MatchActivityAttributes"
      via: "requests Activity on match start, updates ContentState on each point"
      pattern: "Activity.*request|activity.*update"
    - from: "BadmintonEyeLiveActivityLiveActivity.swift"
      to: "MatchActivityAttributes"
      via: "renders lock screen and Dynamic Island from ContentState"
      pattern: "ActivityConfiguration|dynamicIsland"
---

<objective>
Live Activity on iPhone lock screen and Dynamic Island showing live match scores

Purpose: Let users glance at their lock screen or Dynamic Island to see the current match score without opening the app. Auto-starts/stops with match lifecycle.
Output: ActivityKit widget extension with lock screen and Dynamic Island views, LiveMatchViewModel integration
</objective>

<execution_context>
@/Users/milai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/milai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cloud-sync-and-authentication/04-CONTEXT.md

@BadmintonEye/BadmintonEye/ViewModels/LiveMatchViewModel.swift
@BadmintonEye/BadmintonEye/App/BadmintonEyeApp.swift

<interfaces>
<!-- LiveMatchViewModel -- needs Live Activity start/update/end hooks -->

From BadmintonEye/BadmintonEye/ViewModels/LiveMatchViewModel.swift:
```swift
@Observable
final class LiveMatchViewModel {
    private(set) var state: MatchState
    private var persistedMatch: PersistedMatch
    private let modelContext: ModelContext

    // Called on every point, undo, and abandon
    private func persistState() {
        // ... encodes state, updates persistedMatch, sends Watch sync
        // Live Activity updates should be added here
    }

    init(state: MatchState, modelContext: ModelContext) {
        // New match -- Live Activity should start here
    }

    init(restoringState: MatchState, persistedMatch: PersistedMatch, modelContext: ModelContext) {
        // Crash recovery -- Live Activity should restart here
    }
}
```

From ScoringEngine (MatchState):
```swift
struct MatchState {
    var scoreA: Int  // current game score
    var scoreB: Int
    var currentGame: GameState
    var games: [GameState]  // completed games
    var matchPhase: MatchPhase  // .inProgress, .complete, .abandoned
    var matchWinner: Side?
    var serverSide: Side
    var teamANames: [String]
    var teamBNames: [String]
    var format: MatchFormat  // .singles, .doubles, .mixed
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MatchActivityAttributes and Live Activity widget extension</name>
  <files>BadmintonEye/BadmintonEyeLiveActivity/MatchActivityAttributes.swift, BadmintonEye/BadmintonEyeLiveActivity/BadmintonEyeLiveActivityLiveActivity.swift, BadmintonEye/BadmintonEyeLiveActivity/BadmintonEyeLiveActivityBundle.swift, BadmintonEye/BadmintonEye.xcodeproj/project.pbxproj</files>
  <read_first>BadmintonEye/BadmintonEye.xcodeproj/project.pbxproj, BadmintonEye/BadmintonEye/ViewModels/LiveMatchViewModel.swift</read_first>
  <action>
Create the Live Activity widget extension at BadmintonEye/BadmintonEyeLiveActivity/:

1. MatchActivityAttributes.swift:
   - Struct conforming to ActivityAttributes
   - Static attributes: `teamAName: String`, `teamBName: String`, `format: String` (singles/doubles/mixed)
   - ContentState: `scoreA: Int`, `scoreB: Int`, `gameNumber: Int` (1-indexed current game), `gamesWonA: Int`, `gamesWonB: Int`, `serverSide: String` ("sideA" or "sideB"), `isComplete: Bool`

2. BadmintonEyeLiveActivityLiveActivity.swift:
   - Widget conforming to Widget protocol with ActivityConfiguration for MatchActivityAttributes
   - Lock screen expanded view (per user decision -- mirrors Watch glanceable display):
     * Player/team names at top (teamAName vs teamBName)
     * Large current score centered (e.g., "15 - 12") using .system(.title) font
     * Game indicator dots below score (filled for games won, hollow for remaining -- up to 3)
     * Small server indicator (shuttle icon or arrow) on the serving side
     * "Tap to open" subtle hint at bottom
   - Dynamic Island compact (per user decision): Leading shows teamA score, trailing shows teamB score, displaying "12-9" style in minimal layout
   - Dynamic Island expanded: Similar to lock screen but within the expanded island frame -- scores large, names smaller, game dots
   - Dynamic Island minimal: Just the leading score digit

3. BadmintonEyeLiveActivityBundle.swift:
   - WidgetBundle containing the Live Activity widget

4. Update project.pbxproj:
   - Add BadmintonEyeLiveActivity widget extension target
   - Add all 3 Swift files to the new target's build phase
   - Set INFOPLIST_KEY_NSExtensionPointIdentifier to com.apple.widgetkit-extension
   - Set deployment target matching main app
   - Add to main app's "Embed App Extensions" build phase
   - Import ActivityKit and WidgetKit frameworks
   - The widget extension needs its own Info.plist with NSExtension dict (or use GENERATE_INFOPLIST_FILE = YES)
   - Ensure both the main app target AND the widget extension target can access MatchActivityAttributes (either put it in a shared framework, OR duplicate the small struct in both targets, OR add the file to both targets' Compile Sources)

Note: The simplest approach for sharing MatchActivityAttributes between the main app and widget extension is to add MatchActivityAttributes.swift to BOTH targets' Compile Sources in pbxproj. This avoids needing a shared framework.
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye" && xcodebuild -project BadmintonEye/BadmintonEye.xcodeproj -scheme BadmintonEye -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>BadmintonEyeLiveActivity directory contains 3 Swift files; MatchActivityAttributes has ActivityAttributes conformance with ContentState; widget renders lock screen and Dynamic Island layouts; project builds</acceptance_criteria>
  <done>Widget extension target compiles with Live Activity rendering lock screen (large score, names, game dots, server) and Dynamic Island compact (score only)</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Live Activity lifecycle into LiveMatchViewModel</name>
  <files>BadmintonEye/BadmintonEye/ViewModels/LiveMatchViewModel.swift</files>
  <read_first>BadmintonEye/BadmintonEye/ViewModels/LiveMatchViewModel.swift, BadmintonEye/BadmintonEyeLiveActivity/MatchActivityAttributes.swift</read_first>
  <action>
Modify LiveMatchViewModel to manage Live Activity lifecycle:

1. Add `import ActivityKit` at top of file.

2. Add private property: `private var currentActivity: Activity<MatchActivityAttributes>?`

3. Add `startLiveActivity()` method:
   - Guard `ActivityAuthorizationInfo().areActivitiesEnabled`
   - Create MatchActivityAttributes with teamAName (state.teamANames.first ?? "Side A"), teamBName, format
   - Create initial ContentState from current state (scoreA: state.currentGame.scoreA, etc.)
   - Call `try? Activity.request(attributes:content:pushType: nil)` -- no push token needed, updates are local
   - Store returned activity in `currentActivity`

4. Add `updateLiveActivity()` method:
   - Guard `let activity = currentActivity`
   - Build ContentState from current state: scoreA, scoreB from currentGame, gameNumber = games.count + 1 (if in progress), gamesWonA/B from completed games, serverSide from state.serverSide.rawValue, isComplete from matchPhase
   - Call `Task { await activity.update(ActivityContent(state: contentState, staleDate: nil)) }`

5. Add `endLiveActivity()` method:
   - Guard `let activity = currentActivity`
   - Build final ContentState with isComplete = true and final scores
   - Call `Task { await activity.end(ActivityContent(state: finalState, staleDate: nil), dismissalPolicy: .after(.now + 300)) }` -- keeps on lock screen for 5 minutes after match ends
   - Set `currentActivity = nil`

6. Integration points (per user decision -- auto-starts/ends, no user action needed):
   - In `init(state:modelContext:)` (new match): call `startLiveActivity()` after `persistState()`
   - In `init(restoringState:persistedMatch:modelContext:)` (crash recovery): call `startLiveActivity()` to resume the Live Activity
   - In `persistState()`: after the existing code, call `updateLiveActivity()` when matchPhase == .inProgress, or `endLiveActivity()` when matchPhase == .complete or .abandoned
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye" && xcodebuild -project BadmintonEye/BadmintonEye.xcodeproj -scheme BadmintonEye -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>LiveMatchViewModel imports ActivityKit; startLiveActivity called in both init methods; updateLiveActivity called in persistState; endLiveActivity called on match complete/abandon; project builds</acceptance_criteria>
  <done>Live Activity starts on match begin, updates on every point/undo, ends on match complete/abandon with 5-minute dismissal delay</done>
</task>

</tasks>

<verification>
- Project builds with `xcodebuild build` targeting iOS Simulator
- Widget extension target exists in project.pbxproj
- MatchActivityAttributes.swift defines ActivityAttributes with ContentState
- LiveMatchViewModel.swift imports ActivityKit and manages Activity lifecycle
- Lock screen view renders score, names, game dots, server indicator
- Dynamic Island compact shows minimal score layout
</verification>

<success_criteria>
- Live Activity widget extension compiles as part of the project
- LiveMatchViewModel starts Live Activity on match start (both new and restored)
- Every point/undo triggers a Live Activity content update
- Match completion or abandonment ends the Live Activity
- Lock screen shows: large score, player names, game indicator dots, server
- Dynamic Island compact shows: score in "12-9" format
</success_criteria>

<output>
After completion, create `.planning/phases/04-cloud-sync-and-authentication/04-02-SUMMARY.md`
</output>
