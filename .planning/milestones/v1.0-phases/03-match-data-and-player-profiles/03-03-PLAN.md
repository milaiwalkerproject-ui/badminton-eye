---
phase: 03-match-data-and-player-profiles
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - BadmintonEye/BadmintonEye/Views/ScorecardRenderer.swift
  - BadmintonEye/BadmintonEye/Views/MatchExportView.swift
  - BadmintonEye/BadmintonEye/Views/MatchDetailView.swift
autonomous: true
requirements:
  - DATA-06
  - DATA-07

must_haves:
  truths:
    - "User can share a court-themed scorecard image via the system share sheet"
    - "User can export match data as CSV (one row per match, importable by spreadsheet apps)"
    - "User can export match data as a styled PDF formatted for printing"
  artifacts:
    - path: "BadmintonEye/BadmintonEye/Views/ScorecardRenderer.swift"
      provides: "Renders scorecard as UIImage and PDF"
      min_lines: 80
    - path: "BadmintonEye/BadmintonEye/Views/MatchExportView.swift"
      provides: "CSV generation and format picker"
      min_lines: 50
  key_links:
    - from: "BadmintonEye/BadmintonEye/Views/MatchDetailView.swift"
      to: "ScorecardRenderer"
      via: "Toolbar share button triggers rendering"
      pattern: "ScorecardRenderer\\|ShareLink\\|shareSheet"
    - from: "BadmintonEye/BadmintonEye/Views/MatchDetailView.swift"
      to: "MatchExportView"
      via: "Toolbar export button presents format picker"
      pattern: "MatchExportView\\|exportCSV\\|exportPDF"
---

<objective>
Add match result sharing as a court-themed scorecard image and data export as CSV and PDF files.

Purpose: Lets players share results socially and export data for personal records. Completes the match data feature set.
Output: ScorecardRenderer.swift (image + PDF generation), MatchExportView.swift (CSV + format picker), updated MatchDetailView toolbar
</objective>

<execution_context>
@/Users/milai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/milai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-match-data-and-player-profiles/03-CONTEXT.md
@.planning/phases/03-match-data-and-player-profiles/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From BadmintonEye/BadmintonEye/Models/SwiftDataModels.swift:
```swift
@Model
final class PersistedMatch {
    var id: UUID = UUID()
    var format: String = "singles"
    var startedAt: Date = Date()
    var endedAt: Date?
    var stateJSON: Data?
    var isComplete: Bool = false
    var playerAName: String?
    var playerBName: String?
    var playerA2Name: String?
    var playerB2Name: String?
    var winnerSide: String?
    var game1ScoreA: Int = 0
    var game1ScoreB: Int = 0
    var game2ScoreA: Int?
    var game2ScoreB: Int?
    var game3ScoreA: Int?
    var game3ScoreB: Int?
}
```

From BadmintonEye/BadmintonEye/Views/MatchDetailView.swift:
```swift
// Created in Plan 01
// Shows game-by-game breakdown
// Has .toolbar placeholder for share/export buttons
struct MatchDetailView: View {
    let match: PersistedMatch
    // ...
}
```

From BadmintonEye/BadmintonEye/Models/CodableMatchState.swift:
```swift
struct CodableMatchState: Codable {
    let format: MatchFormat
    var games: [GameState]
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scorecard image renderer and PDF generator</name>
  <read_first>
    - BadmintonEye/BadmintonEye/Models/SwiftDataModels.swift
    - BadmintonEye/BadmintonEye/Models/CodableMatchState.swift
    - BadmintonEye/BadmintonEye/Views/MatchEndView.swift
  </read_first>
  <files>BadmintonEye/BadmintonEye/Views/ScorecardRenderer.swift</files>
  <action>
    Create `BadmintonEye/BadmintonEye/Views/ScorecardRenderer.swift` with a struct that renders match data into shareable formats:

    ```swift
    struct ScorecardRenderer {
        static func renderImage(for match: PersistedMatch) -> UIImage?
        static func renderPDF(for match: PersistedMatch) -> Data?
    }
    ```

    **Image rendering (for share sheet):**
    - Use `UIGraphicsImageRenderer(size: CGSize(width: 600, height: 400))` for a card-sized image
    - Draw a court-themed background: dark green (#1B5E20) rounded rectangle with subtle court line pattern (two white horizontal lines at 1/3 and 2/3 height to suggest a court net)
    - Top: "Badminton Eye" text in white, small font (system 12, .semibold)
    - Center: player names in white, large font (system 22, .bold), "vs" between them
    - For doubles: "Alice & Bob" format
    - Below names: game scores in a row. Each game as "21-18" in white (system 18), separated by commas. Winner's score bolded.
    - Bottom-left: date formatted as "Mar 28, 2026"
    - Bottom-right: match format badge ("Singles" / "Doubles" / "Mixed") in a small rounded pill
    - Bottom-center: "Badminton Eye" watermark in small, semi-transparent white text (alpha 0.5)
    - Use `match.winnerSide` to determine winner; highlight winner name with a subtle glow or underline

    **PDF rendering (for export/printing):**
    - Use `UIGraphicsPDFRenderer(bounds: CGRect(x: 0, y: 0, width: 612, height: 792))` for US Letter size
    - Single page with:
      - Title: "Match Scorecard" centered, bold 24pt
      - Subtitle: date + format, centered, 14pt gray
      - Horizontal rule
      - Player names section: Team A vs Team B with all player names
      - Score table: game-by-game with "Game 1", "Game 2", "Game 3" rows, columns for each team
      - Draw table with lines using CGContext strokes
      - Winner row at bottom with bold formatting
      - Footer: "Generated by Badminton Eye" + date, small gray text
    - Decode stateJSON to CodableMatchState for full game data; fall back to game1/game2/game3 scores if decode fails

    All drawing uses CoreGraphics (UIKit). No SwiftUI rendering or third-party libraries. Use `NSAttributedString.draw(in:)` for styled text placement.
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye" && wc -l BadmintonEye/BadmintonEye/Views/ScorecardRenderer.swift && grep -c "UIGraphicsImageRenderer\|UIGraphicsPDFRenderer\|renderImage\|renderPDF" BadmintonEye/BadmintonEye/Views/ScorecardRenderer.swift</automated>
  </verify>
  <acceptance_criteria>
    - ScorecardRenderer.swift exists with 80+ lines
    - Has static `renderImage(for:) -> UIImage?` using UIGraphicsImageRenderer, 600x400 card, green court background, white text for names/scores, date, format badge, watermark
    - Has static `renderPDF(for:) -> Data?` using UIGraphicsPDFRenderer, Letter size, title/date/player names/score table/footer
    - Both methods decode stateJSON for full game data with fallback to persisted game scores
    - No third-party dependencies, pure CoreGraphics/UIKit
  </acceptance_criteria>
  <done>Scorecard renderer produces a court-themed shareable image and a printable PDF from any PersistedMatch</done>
</task>

<task type="auto">
  <name>Task 2: CSV export, format picker, and toolbar integration in match detail</name>
  <read_first>
    - BadmintonEye/BadmintonEye/Views/MatchDetailView.swift
    - BadmintonEye/BadmintonEye/Views/ScorecardRenderer.swift
  </read_first>
  <files>
    BadmintonEye/BadmintonEye/Views/MatchExportView.swift,
    BadmintonEye/BadmintonEye/Views/MatchDetailView.swift
  </files>
  <action>
    1. Create `BadmintonEye/BadmintonEye/Views/MatchExportView.swift`:
       - A view/utility that handles CSV generation and export format selection
       - CSV generation function:
         ```swift
         static func generateCSV(for matches: [PersistedMatch]) -> String
         ```
         - Header row: "Date,Format,Player A,Player A2,Player B,Player B2,Game 1 A,Game 1 B,Game 2 A,Game 2 B,Game 3 A,Game 3 B,Winner"
         - One row per match with values from PersistedMatch properties
         - Winner determined from winnerSide: if "sideA" use playerAName, if "sideB" use playerBName
         - Escape commas in player names with quotes
         - Also support single-match CSV: `static func generateCSV(for match: PersistedMatch) -> String`

       - Format picker view (presented as action sheet or confirmation dialog):
         ```swift
         struct ExportFormatPicker: View {
             let match: PersistedMatch
             @Binding var isPresented: Bool
             // Shows options: "Share Scorecard Image", "Export as CSV", "Export as PDF"
         }
         ```
         Each option triggers the appropriate generation and presents UIActivityViewController via ShareLink or a custom share sheet wrapper.

    2. Update `BadmintonEye/BadmintonEye/Views/MatchDetailView.swift`:
       - Add toolbar buttons:
         - Share button (square.and.arrow.up icon): generates scorecard image via `ScorecardRenderer.renderImage(for:)` and presents ShareLink or UIActivityViewController
         - Export button (doc.text icon): presents the format picker (CSV or PDF options)
       - For share: use SwiftUI `ShareLink` with a `Transferable` item if possible, or fall back to wrapping UIActivityViewController in a UIViewControllerRepresentable
       - For CSV export: generate CSV string, write to temp file in `FileManager.default.temporaryDirectory` with `.csv` extension, share via activity controller
       - For PDF export: call `ScorecardRenderer.renderPDF(for:)`, write to temp file with `.pdf` extension, share via activity controller
       - Add `@State` properties for controlling sheet presentation: `showExportPicker`, `showShareSheet`
       - Implementation approach for sharing: create a small `ActivityViewController` UIViewControllerRepresentable wrapper that takes `[Any]` items and presents UIActivityViewController. This is more reliable than ShareLink for UIImage and file URLs.
  </action>
  <verify>
    <automated>cd "/Users/milai/Desktop/badminton eye" && wc -l BadmintonEye/BadmintonEye/Views/MatchExportView.swift && grep -c "generateCSV\|CSV\|exportPDF\|ExportFormatPicker" BadmintonEye/BadmintonEye/Views/MatchExportView.swift && grep -c "ScorecardRenderer\|MatchExportView\|shareSheet\|exportPicker\|toolbar" BadmintonEye/BadmintonEye/Views/MatchDetailView.swift</automated>
  </verify>
  <acceptance_criteria>
    - MatchExportView.swift exists with 50+ lines, has `generateCSV(for:)` producing valid CSV with headers and escaped values, ExportFormatPicker view with Image/CSV/PDF options
    - MatchDetailView.swift updated with toolbar containing share button (scorecard image) and export button (CSV/PDF picker)
    - Share presents UIActivityViewController with rendered scorecard UIImage
    - CSV writes to temp file and shares via activity controller
    - PDF writes to temp file and shares via activity controller
    - ActivityViewController wrapper (UIViewControllerRepresentable) handles sharing
  </acceptance_criteria>
  <done>Users can share scorecard images and export match data as CSV or PDF from the match detail toolbar</done>
</task>

</tasks>

<verification>
- Build the project: `xcodebuild build` should succeed
- ScorecardRenderer has both image and PDF rendering functions
- MatchDetailView toolbar has share and export buttons
- CSV output has correct header row and data format
</verification>

<success_criteria>
- Court-themed scorecard image (600x400, green background, player names, scores, watermark) shareable via system share sheet
- CSV export with header row, one row per match, all fields, importable by spreadsheet apps
- Styled PDF formatted for A4/Letter with title, players, score table, generated by Badminton Eye footer
- Share and export accessible from match detail toolbar
</success_criteria>

<output>
After completion, create `.planning/phases/03-match-data-and-player-profiles/03-03-SUMMARY.md`
</output>
