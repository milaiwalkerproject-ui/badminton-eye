import SwiftUI

/// Placeholder for court calibration UI.
/// Full implementation in a subsequent Hawk Eye plan.
struct CourtCalibrationView: View {
    var body: some View {
        Text("Court Calibration")
            .navigationTitle("Calibrate Court")
    }
}
